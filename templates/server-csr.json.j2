{
    "CN": "{{ inventory_hostname }}",
    "hosts": [
      {% for item in auth_hosts[inventory_hostname] %}
      "{{ item }}"{% if not loop.last %},{% endif %}
      {% endfor %}
    ],
    "key": {
        "algo": "{{ cert_key_algo }}",
        "size": {{ cert_key_size }}
    },
    "names": [
        {
          "C": "{{servercert.C}}",
          "L": "{{servercert.L}}",
          "ST": "{{servercert.ST}}",
          "O": "{{servercert.O}}",
          "OU": "{{servercert.OU}}"
        }
    ]
}
