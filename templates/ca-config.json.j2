{
  "signing": {
    "default": {
      "expiry": "{{ca.expiry}}"
    },
    "profiles": {
      "{{ca.profile}}": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "{{ca.expiry}}"
      }
    }
  }
}

